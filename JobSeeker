import socket

sock = socket.socket()
host = socket.gethostname()
port = 7777

sock.connect((host,port))
print (sock.recv(1024))

sock.send("I have resources, looking for work!".encode())

sock.close()
